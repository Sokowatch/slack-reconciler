{
  "name": "Slack reconciler",
  "description": "Slack Webhook bridge/passthrough using sinatra & slack-notifier",
  "repository": "https://github.com/Reliefwatch/slack-reconciler",
  "keywords": [
    "slack",
    "webhook",
    "sinatra"
  ],
  "env": {
    "SLACK_URL": "REQUIRED: SLACK Webhook URL",
    "SLACK_CHANNEL": "OPTIONAL: Defaults to the channel defined in the slack integration",
    "SLACK_USERNAME": "OPTIONAL: Defaults to the username defined in the slack integration"
  }
}
